{
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0",
    "prettier": "^3.1.1"
  },
  "scripts": {
    "prepare": "husky install",
    "dev": "cd frontend && yarn dev",
    "all": "yarn && cd frontend && yarn && ../backend && yarn"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  }
}
